import { HideEyeDirective } from './hide-eye.directive';

describe('HideEyeDirective', () => {
  it('should create an instance', () => {
    const directive = new HideEyeDirective();
    expect(directive).toBeTruthy();
  });
});
